[project]
name = "living-planet-simulator"
version = "0.1.0"
description = "Minimal platform architecture for a planetary simulator"
requires-python = ">=3.11"

[tool.poetry.dependencies]
python = ">=3.11"
fastapi = "^0.111.0"
uvicorn = "^0.23.0"
